{% form_theme form 'form/form_errors.html.twig' %}
{% extends 'mainPage.html.twig' %}

{% block main %}
    
    <div class="col-md-8 col-md-offset-2">
        <h2>Coaching einfügen</h2>        
        
            {{ form_start(form) }}
			{{ form_errors(form) }}
			
			<div class="form-group row">
				{{ form_errors(form.patient) }}
				{{ form_label(form.patient) }}
				<div class="col-sm-10">
					{{ form_widget(form.patient) }}
				</div>
			</div>

			<div class="form-group row">
				{{ form_errors(form.sysUser) }}
				{{ form_label(form.sysUser) }}
				<div class="col-sm-10">
					{{ form_widget(form.sysUser) }}
				</div>
			</div>

			<div id="dateTimePickerContainer" class="form-group row">
				{{ form_errors(form.dateAndTime) }}
				{{ form_label(form.dateAndTime) }}
				<div class="col-sm-10">
	                <div class="input-group time">
	        			{{ form_widget(form.dateAndTime) }}
	        			<span class="input-group-addon"><i class="glyphicon glyphicon-calendar"></i></span>
					</div>
            	</div>
			</div>

			<div class="form-group row">
				{{ form_errors(form.weekGoal) }}
				{{ form_label(form.weekGoal) }}
				<div class="col-sm-10">
					{{ form_widget(form.weekGoal) }}
				</div>
			</div>
			
		{{ form_end(form) }}
	</div>

{% endblock %}